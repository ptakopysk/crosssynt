# Memory limit
#$ -l h_vmem=3g
#$ -l mem_free=3g

echo -ne "$t.tagger.wtcproj.int.udpipe\t"
cat $tgtdtest | \
    $udpipe --tag --accuracy \
    models/$t.tagger.wtcproj.int.udpipe | \
    grep -o 'upostag: [0-9.]*'| cut -d' ' -f2
